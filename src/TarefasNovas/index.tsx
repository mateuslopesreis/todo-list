

type ListagemTarefasProps ={
    children:React.ReactNode
}

export function TarefasNovas({children}:ListagemTarefasProps){
    return(

        <section className='listandoContatos'>
   
                    {children}
            
 
       
        </section>
    )
}